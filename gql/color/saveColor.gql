mutation saveColor(
  $id: String!
  $name: String
  $color_code: String
  $info: String
  $featured: Boolean
  $active: Boolean
  $store: ID
) {
  saveColor(
    id: $id
    name: $name
    color_code: $color_code
    info: $info
    featured: $featured
    active: $active
    store: $store
  ) {
    id
    name
    color_code
    info
    featured
    active
    store
    createdAt
    updatedAt
  }
}
